<div>
  <div class="lists-banner">
    <h2>Mes listes</h2>
  </div>
    <%= link_to "Nouvelle liste", new_list_path, id: "button-lists", class: "btn btn-primary" %>
    <div class="lists-grid container">
      <% if @lists.present? %>
        <% @lists.each do |list| %>
          <%= link_to list_path(list) do  %>
            <div class="list-card" style="">
              <%= list.name.upcase %>
            </div>
          <% end %>
        <% end %>
      <% else %>
        <p>C'est le moment de se créer une liste !!</p>
      <% end %>
    </div>
</div>
